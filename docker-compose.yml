version: '3.8'

services:

  web-customer:
    image: m-cafe-web-customer
    build:
      context: ./
      dockerfile: ./services/web/Dockerfile
      target: run-prod-web-customer
      args:
        - BACKEND_URL=http://localhost:8080/api
        - DOMAIN_URL=http://localhost:8080
    container_name: m-cafe-web-customer
    env_file:
      - ./docker-envs/.docker.validation-overrides.env
    depends_on:
      - nginx

  web-admin:
    image: m-cafe-web-admin
    build:
      context: ./
      dockerfile: ./services/web/Dockerfile
      target: run-prod-web-admin
      args:
        - BACKEND_URL=http://localhost:8080/api
        - DOMAIN_URL=http://localhost:8080
    container_name: m-cafe-web-admin
    env_file:
      - ./docker-envs/.docker.validation-overrides.env
    depends_on:
      - nginx

  web-manager:
    image: m-cafe-web-manager
    build:
      context: ./
      dockerfile: ./services/web/Dockerfile
      target: run-prod-web-manager
      args:
        - BACKEND_URL=http://localhost:8080/api
        - DOMAIN_URL=http://localhost:8080
    container_name: m-cafe-web-manager
    env_file:
      - ./docker-envs/.docker.validation-overrides.env
    depends_on:
      - nginx

  redis:
    image: m-cafe-redis
    build:
      context: ./
      dockerfile: ./redis/Dockerfile
    container_name: m-cafe-redis
    restart: always
    command: ["redis-server", "/etc/redis/redis.conf"]
    # #######################
    # NOT IMPLEMENTED ssl/tls tunnelling for redis
    # command: sh -c "stunnel /stunnel_take2.conf && /usr/local/bin/redis-server /etc/redis/redis.conf"
    # #######################
    ports:
      - 6379:6379
    volumes:
      - ./redis/data:/data
      - ./redis/certs:/etc/certs
      - ./redis/redis.conf:/etc/redis/redis.conf:ro

  backend:
    image: m-cafe-backend
    build:
      context: ./
      dockerfile: ./services/backend/Dockerfile
    container_name: m-cafe-backend
    env_file:
      - ./docker-envs/.docker-back.env
      - ./docker-envs/.docker.validation-overrides.env
    depends_on:
      - nginx
      - redis

  nginx:
    image: nginx:1.25.1
    restart: unless-stopped
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    ports:
      - 8080:80
    container_name: m-cafe-reverse-proxy

volumes:
  pgdata:
